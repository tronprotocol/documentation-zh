# GetAssetIssueList

查询全网所有 TRC10 token。

- 服务：同时支持 `Wallet` 和 `WalletSolidity`

```protobuf
rpc GetAssetIssueList (EmptyMessage) returns (AssetIssueList) {}
```

相似 HTTP 接口见 [/wallet/getassetissuelist](../../http/asset/getassetissuelist.md)。

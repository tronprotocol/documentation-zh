# GetNowBlock2

返回当前最新区块。

- 服务：同时支持 `Wallet` 和 `WalletSolidity`

```protobuf
rpc GetNowBlock2 (EmptyMessage) returns (BlockExtention) {}
```

相似 HTTP 接口见 [/wallet/getnowblock](../../http/block-and-tx-query/getnowblock.md)。

# UpdateBrokerage

SR 修改分红比例（佣金）。

- 服务：仅支持 `Wallet`

```protobuf
rpc UpdateBrokerage (UpdateBrokerageContract) returns (TransactionExtention) {}
```

相似 HTTP 接口见 [/wallet/updateBrokerage](../../http/witness-and-governance/updateBrokerage.md)。

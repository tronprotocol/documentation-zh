# UpdateEnergyLimit

修改合约 `origin_energy_limit`（仅部署者）。

- 服务：仅支持 `Wallet`

```protobuf
rpc UpdateEnergyLimit (UpdateEnergyLimitContract) returns (TransactionExtention) {}
```

相似 HTTP 接口见 [/wallet/updateenergylimit](../../http/smart-contract/updateenergylimit.md)。
